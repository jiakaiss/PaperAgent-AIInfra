# Paper Agent 配置文件
# 复制为 config.yaml 并填入你的实际配置
# 敏感信息可使用 ${ENV_VAR} 从环境变量读取
#
# 用户来源：所有论文推送用户都通过 Web 表单订阅（/subscribe）添加，存入
# SQLite 的 subscriptions 表。本配置文件不再需要 users 列表。

# ─── arXiv 抓取配置（全局共享） ───
fetch:
  categories:
    - cs.DC     # 分布式计算
    - cs.LG     # 机器学习
    - cs.AI     # 人工智能
    - cs.PF     # 性能
    - cs.NI     # 网络
    - cs.AR     # 硬件架构
  keywords:
    - distributed training
    - model parallelism
    - pipeline parallelism
    - tensor parallelism
    - inference optimization
    - model serving
    - GPU scheduling
    - large language model infrastructure
    - mixture of experts
    - speculative decoding
    - KV cache
    - quantization
    - model compression
    - all-reduce
    - communication optimization
    - memory optimization
    - deep learning compiler
    - operator fusion
  max_results: 200
  days_back: 2

  # ─── 双轨检索（质量底线策略） ───
  # "none"             —— 兼容老行为：所有关键词共享 max_results 预算，
  #                       一个噪声关键词（如 "serving"）可能挤掉小众关键词。
  # "per_keyword_cap"  —— 开启双轨：
  #                       • 轨道1：每个关键词独立查询，单独配额，避免独占；
  #                       • 轨道2：再按 cross_list_categories 拉一遍近期论文，
  #                         补上"用了别的术语，关键词没命中"的高质量工作。
  # 新部署推荐改为 per_keyword_cap，老配置默认保持 none 不变行为。
  quality_floor_strategy: none
  # 每个关键词查询的最小返回数（仅在 per_keyword_cap 下生效）。
  # 实际配额 = max(min_per_keyword, max_results // num_queries)。
  min_per_keyword: 10
  # 轨道2 使用的 arXiv 分类（仅在 per_keyword_cap 下生效）。
  # 留空 [] 等于禁用轨道2；新部署推荐 [cs.LG, cs.DC]。
  cross_list_categories: []

# ─── LLM 打分配置（全局共享） ───
scoring:
  model: claude-haiku-4-5       # 模型名称（可换成为其他 Anthropic 模型）
  batch_size: 10                 # 每次 API 调用处理的论文数

  # ─── API 连接配置 ───
  # api_key: 留空则使用环境变量 ANTHROPIC_API_KEY（推荐通过 ${ENV_VAR} 引用）
  api_key: ${ANTHROPIC_API_KEY}
  # base_url: 自定义 API 端点（用于代理 / 网关），留空使用 Anthropic 官方端点
  # base_url: https://your-proxy.example.com/v1

  # ─── 生成参数 ───
  max_tokens: 4096               # 每次 API 调用的最大输出 token 数
  # temperature: 0.3             # 留空使用 SDK 默认（不设置该参数）
  tool_choice: auto              # "auto" 或 "tool"（强制使用 score_papers 工具）
  abstract_max_length: 800       # 提交给 LLM 前截断摘要的字符数

  # ─── 评分权重 ───
  # total_score = relevance * relevance_weight + quality * quality_weight
  # 建议两者之和为 1.0，不为 1.0 时会输出警告但仍可使用
  relevance_weight: 0.6          # 相关性权重
  quality_weight: 0.4            # 质量权重
  # citation_weight：引用数对排序的影响。0.0（默认）= 引用不参与排序，
  # 与本变更前完全一致。> 0 时启用「同 tier、同 total_score」的引用 tiebreaker
  # （不进入 total_score 公式，仅作排序兜底）。需同时启用 citations.enabled。
  citation_weight: 0.0

  # ─── Prompt 自定义（可选） ───
  # 留空或未设置时使用内置默认 prompt
  # prompts:
  #   system_prompt: |
  #     你是一个专注于 AI Infrastructure 的审稿人。请对论文的相关性和质量打分 (0-10)。
  #   # 用户消息模板支持 {paper_count} 和 {papers} 两个占位符
  #   user_message_template: |
  #     请对以下 {paper_count} 篇论文打分，并使用 score_papers 工具返回结果。
  #
  #     {papers}

# ─── 全局邮件配置 ───
# 邮件是唯一支持的推送渠道。通过 Web 表单订阅的所有用户都使用这里的 SMTP 配置。
# 如果未配置此项，订阅用户将无法收到邮件（但仍可在 Web 界面浏览论文）。
email:
  enabled: false                 # 设为 true 以启用邮件推送功能
  smtp_host: smtp.gmail.com      # SMTP 服务器地址
  smtp_port: 587                 # SMTP 端口（587 for TLS, 465 for SSL）
  smtp_user: ${SMTP_USER}        # SMTP 用户名（通常就是你的邮箱地址）
  smtp_password: ${SMTP_PASSWORD} # SMTP 密码或应用专用密码
  sender: ${SMTP_SENDER}         # 发件人邮箱（通常与 smtp_user 相同）
  use_tls: true                  # 是否使用 TLS 加密

# ─── 全局推送阈值 ───
# 适用于所有通过 Web 订阅的用户。订阅时用户在 Web 表单选择感兴趣的 sub_domains，
# 其他过滤条件全部在此统一配置。
#
# 可用的子领域 (sub_domains)：
#   quantization, distillation, pruning, sparsity, distributed_training,
#   parallelism, serving, speculative_decoding, kv_cache, moe, compiler,
#   memory_optimization, communication, scheduling
thresholds:
  min_relevance: 6.0             # 相关性分数下限（0-10）
  min_quality: 5.0               # 质量分数下限（0-10）
  top_n: 50                      # 每次推送最多论文数
  per_sub_domain_top_n: 20       # 每个订阅领域独立取 top N 后合集去重；订阅 ["all"] 时不生效
  min_tier: solid                # 影响力阈值；"breakthrough" = 仅重磅工作，
                                 # "solid"（默认）= 排除渐进改进，
                                 # "incremental" = 包含全部。
  # 重要老作分区（重要老作 / Important Older Works）。
  # > 0 时每次推送在常规论文之外额外附带 N 篇高引老论文（不占用 top_n 名额）。
  # 默认 0 = 关闭。需同时启用 citations.enabled。
  older_works_per_digest: 0
  # 老作引用数门槛：S2 引用数 ≥ 该值的老论文才会被视为「重要老作」候选。
  min_citations_for_older_works: 100

# ─── 引用数采集与动态评分（可选，默认关闭） ───
# 启用后会做三件事：
#   1) 周期性从 Semantic Scholar 拉取每篇缓存论文的引用数；
#   2) 引用数显著上涨时，让 Claude 重新评估 relevance/quality/tier
#      （total_score 公式不变，但分数随重判结果动态变化）；
#   3) 每次 ingest 反向发现高引老论文，加入「重要老作」分区。
# 默认 enabled=false 等于全部禁用，行为与本变更前完全一致。
citations:
  enabled: false                          # 一键启用所有引用相关功能
  provider: semantic_scholar              # 当前仅支持 semantic_scholar

  # ── 数据源连接 ──
  # api_key: 留空也能用（公开配额 ~100 req/min），有 key 时配额更高
  # api_key: ${S2_API_KEY}
  base_url: https://api.semanticscholar.org/graph/v1
  request_timeout: 15.0                   # 单请求超时（秒）
  batch_size: 50                          # S2 batch 接口一次问几篇
  requests_per_second: 1.0                # S2 限速，太高会被 429

  # ── 刷新节奏 ──
  refresh_interval_hours: 24              # 多久采一次引用数
  refresh_candidate_limit: 500            # 每次刷新最多看几篇候选

  # ── 排序 tiebreaker ──
  # 引用数归一化到 0-10 的 log 上限。降到 200 = 中等引用就能拉开差距；
  # 升到 5000 = 只有大热门才显著影响排序。仅在 citation_weight > 0 时生效。
  normalization_ceiling: 1000

  # ── 动态重判（成本敏感）──
  # 触发条件：引用涨幅 ≥ rescore_min_delta 或 ≥ rescore_min_ratio。
  # 成本上限：每次刷新最多 rescore_max_per_run 篇 × 每篇最少间隔 rescore_min_interval_days 天。
  # 设 rescore_max_per_run: 0 完全关闭重判（仍会采集引用数）。
  rescore_min_delta: 50                   # 引用涨幅绝对门槛
  rescore_min_ratio: 0.2                  # 引用涨幅相对门槛（20%）
  rescore_max_per_run: 20                 # 每次最多重判几篇（0 = 关闭）
  rescore_min_interval_days: 7            # 同一篇最小重判间隔

  # ── 老作发现 ──
  older_works_min_age_years: 2            # 至少这么多年以前的论文才算「老作」（下限）
  # 最多多少年以前的论文还能算候选。默认 10 年挡掉 1990s/2000s 的学术
  # 老作（MapReduce/Dryad 之类与今天 AI Infra 关联很弱）。Transformer
  # (2017)、Adam (2014) 这类基础工作仍在窗口内。设大于等于 min。
  older_works_max_age_years: 10
  older_works_max_new_per_ingest: 20      # 每次 ingest 最多打分多少新发现的老作
  older_works_search_page_size: 20        # 每个领域 keyword 取多少 S2 候选
  # 每个 sub_domain 用多少 keyword 变体搜（取自 SUB_DOMAINS dict）。
  # 比如 quantization → ["quantization", "PTQ", "QAT", ...] 取前 N 个。
  # 调高捞得更全（同义词覆盖好），但 S2 调用数线性涨。
  older_works_keywords_per_sub_domain: 3
  # 已缓存的 fresh 论文引用涨到该值且年龄 >= older_works_min_age_years 时,
  # 自动翻牌为 paper_kind="older"。比 min_citations_for_older_works
  # 高一档（discovery 入门 100, 翻牌要 500）— 翻牌是更强的断言。
  older_works_promote_min_citations: 500

# ─── Web 订阅配置 ───
subscriptions:
  default_top_n: 10              # （兼容字段；实际由 thresholds.top_n 控制）
  send_initial_digest_on_signup: true # 订阅成功后立即为该用户推送一次
  access:
    enabled: false               # 设为 true 后，订阅表单需要访问码
    access_codes: []             # 示例: ["private-code"]；不要提交真实访问码
  unsubscribe:
    secret: ${UNSUBSCRIBE_SECRET} # 用于签名取消订阅链接；生产环境请设置稳定随机值
    token_max_age_hours: 720     # 取消订阅链接有效期（默认 30 天）

# ─── Web 展示配置 ───
web:
  min_quality: 5.0               # Web 列表默认隐藏质量分低于该值的论文；设为 0 或 null 可关闭
  public_base_url: ""            # 外部访问地址，用于生成取消订阅链接，如 https://papers.example.com
  admin_contact: ""              # 订阅页"联系管理员"后的联系方式提示；为空时不显示。例如 "admin@example.com" 或 "张三 <admin@example.com>"

# ─── 定时任务配置（全局） ───
schedule:
  enabled: true
  # 后台 ingest 调度有两种模式（二选一）：
  #   - cron 模式（推荐）：设置 ingest_hours，每天在指定小时精确触发
  #   - interval 模式：留空 ingest_hours，按 ingest_interval_minutes 周期触发
  # 用户推送（digest）仍每天 digest_hour:digest_minute 只从缓存发送一次。
  ingest_interval_minutes: 360   # interval 模式下的查询周期；被 ingest_hours 覆盖时忽略
  ingest_hours: [6, 18]          # cron 模式：每天 6:00 和 18:00 各拉一次；置 null 或省略则走 interval
  ingest_minute: 0               # cron 模式下统一的分钟
  digest_hour: 9                 # 每日用户推送小时
  digest_minute: 0               # 每日用户推送分钟
  timezone: Asia/Shanghai

# ─── 存储配置 ───
storage:
  db_path: paper_agent.db    # Docker 部署时改为 /app/data/paper_agent.db

# ─── 管理员看板（可选） ───
# 启用后，/admin 提供一个只有你能访问的运营看板，可查看订阅用户、
# 推送统计、论文库情况和系统状态。HTTP Basic Auth 保护，浏览器会
# 弹出原生登录框。disabled 或 password 为空时所有 /admin* 路径返回
# 404（不会暴露看板存在）。
#
# 注意：admin 看板必须通过 HTTPS 访问，否则 Basic Auth 凭据会明文传输。
admin:
  enabled: false                  # 设为 true 以启用看板
  username: admin                 # Basic Auth 用户名
  # password 支持两种写法：
  #   1. 环境变量（推荐）：password: ${ADMIN_PASSWORD}
  #   2. 直接明文：     password: "your-strong-password"
  # 生成强密码示例：openssl rand -base64 24
  password: ${ADMIN_PASSWORD}

# ─── 日志配置 ───
logging:
  level: INFO
  # 设为文件路径以写入日志文件（推荐 logs/daemon.log，配合 scripts/daemon.sh
  # 持久化运行），null 则仅输出到控制台。daemon 模式下强烈建议设为文件，
  # 否则日志依赖启动 shell 的 stderr 句柄，shell 退出后日志会中断。
  # Docker 部署时改为 /app/logs/paper-agent.log
  file: logs/daemon.log
